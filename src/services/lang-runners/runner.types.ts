/**
 * Re-export of the shared `LangRunner` interface so each runner module imports
 * from this folder rather than reaching across the tree.
 */
export type { LangRunner } from '../../types/leetcode.types.js';
